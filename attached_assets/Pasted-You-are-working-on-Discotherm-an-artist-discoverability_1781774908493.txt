You are working on Discotherm, an artist-discoverability diagnostic tool
submitted to the Musixmatch Musicathon 2026. The running app is a Python/
FastAPI backend with a vanilla HTML/CSS/JS frontend (no framework). It's
judged partly on craft, so the public repo needs to be clean and to match
what the README claims. Work through the tasks below in order. Do NOT break
the running app — after any cleanup, confirm `uvicorn main:app` still starts
and the demo still produces a report. Commit in small, clearly-described
steps attributed to the human author (Lorenzo Porcaro), not the agent.

1. RESOLVE THE TYPESCRIPT DISCREPANCY (highest priority).
   The repo is ~59% TypeScript with package.json, pnpm-workspace.yaml,
   pnpm-lock.yaml, tsconfig.base.json, tsconfig.json, and lib/ +
   discoverability-check/ directories — but the README says the stack is
   Python/FastAPI + vanilla JS with no framework. First, determine what is
   actually used by the running app versus leftover Replit workspace
   scaffolding. Report your findings before deleting anything.
   - If the TS/Node files are dead scaffolding: remove them so the repo's
     language breakdown reflects a Python/HTML/CSS/JS project.
   - If any of it is genuinely used at runtime: leave it and instead update
     the README "Tech stack" section to describe the real architecture
     accurately.
   Either way, the repo and the README must end up consistent.

2. FIX THE "RUNNING LOCALLY" INSTRUCTIONS.
   The README tells users to `pip install -r requirements.txt`, but the repo
   ships pyproject.toml + uv.lock and has no requirements.txt. Update the
   instructions to the correct workflow (e.g. `uv sync`) OR generate a real
   requirements.txt that matches the dependencies — pick whichever is cleaner
   and make sure the documented steps actually work from a fresh clone.

3. MAKE AUTHORSHIP TRANSPARENT.
   Add a short line to the README stating the project was built with Replit
   Agent during the contest week, and credit Lorenzo Porcaro as author/
   maintainer. (Replit is the sponsor, so using their tooling is a positive —
   surface it rather than leaving the repo authored solely by "replit-agent.")

4. CHECK FOR ANYTHING THAT SHOULDN'T BE PUBLIC.
   Inspect .agents/memory/ and attached_assets/ for stray context, internal
   notes, or anything not meant for a public submission. Confirm no API keys
   are committed anywhere and that .gitignore covers .env. Report what you find
   before removing anything.

5. ADD A LICENSE (lower priority).
   The repo has no LICENSE file. Propose 2–3 options with their implications
   (note that contest rules let the author retain ownership) and let me choose
   before committing one — don't auto-pick.

When done, give me a summary of: what the TypeScript turned out to be, what you
removed or changed, and confirmation the app still runs end-to-end.