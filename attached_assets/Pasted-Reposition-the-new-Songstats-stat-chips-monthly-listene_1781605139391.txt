Reposition the new Songstats stat chips (monthly listeners, followers, popularity score) in index.html so they appear directly below the artist name/thumbnail row, but before the overall score thermometer section. Current likely order is wrong if chips ended up after the diagnostics or after the score — confirm the visual order top to bottom is: (1) artist thumbnail + name + track count, (2) stat chips row, (3) overall score thermometer, (4) diagnostic cards. Adjust the animation stagger timing so the chips animate in right after the header appears and before the thermometer count-up begins, rather than all elements animating simultaneously. Test with Sufjan Stevens and confirm the visual order and animation sequence both look correct.



and also



Add a fan-engagement framing line using data already available in raw_stats from Songstats. Pull tiktok_creates (or equivalent field name in raw_stats — check what's actually present) and shazam_count (or equivalent) for the artist. Add a new small section in index.html, positioned between the stat chips row and the overall score thermometer, styled distinctly (not a diagnostic card, more like a highlighted callout/banner) — something like: "🎵 X TikTok creates and Y Shazams show real fans are actively engaging with this music" if either number is meaningfully greater than zero. If both are zero or unavailable, omit this section entirely rather than showing a zero. Also pass these two numbers into the existing PLATFORM_GAP recommendation logic in scoring.py: when an inactive platform is TikTok specifically and tiktok_creates is actually > 0 (meaning there IS fan content but Songstats shows it as below the "meaningful activity" threshold), adjust the recommendation text to say something like "Fans are already creating TikTok content, but this isn't yet translating into platform-level discovery signals — work with a music marketing team to amplify existing fan content rather than starting from zero." Test with Sufjan Stevens, Katy Perry, and i cani — show me whether tiktok_creates/shazam_count data is actually present for each and how the new section renders.