kind = "web"
previewPath = "/discoverability-check"
title = "Discoverability Check"
version = "1.0.0"

[[services]]
localPort = 8000
name = "Discoverability Check"
paths = ["/discoverability-check"]

[services.development]
run = "uvicorn main:app --host 0.0.0.0 --port 8000"
